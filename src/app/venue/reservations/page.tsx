import { VenueStub } from "@/components/venue-stub";

export default function Page() {
  return <VenueStub title="Reservas" note="Vista de reservas por semana y mes. Fuera del alcance de la Fase 0 — el dashboard ya muestra las del día." />;
}
