import { StubScreen } from "@/components/stub-screen";
export default function Page() {
  return <StubScreen title="Perfil" note="Preferencias, billeteras e historial de gasto — fuera del alcance de la Fase 0. El flujo de la demo arranca en Inicio." />;
}
